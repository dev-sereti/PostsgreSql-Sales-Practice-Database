Error:
fatal: dev cannot be resolved to branch

If you’re not sure what branches you actually have, run:
git branch

If the branch already exists locally but you’re not on it, just switch to it first:

git checkout dev
git push -u origin dev
